# Database (3.7GB — rebuild from CSVs with ingest.py + indicators.py)
spy.db
spy.db-wal
spy.db-shm

# Compressed archive (redundant with CSV source files)
spy.zip

# SSH keys
newkey
newkey.pub

# Python
__pycache__/
*.pyc
*.pyo

# Environment
.env
.env.*
!.env.example

# Claude CLI config (keep commands/ for the /new-study skill)
.claude/*
!.claude/commands/

# Screenshots (not essential for the project)
*.jpg
*.png
*.webp
!validated-backtests/*.jpeg
!validated-backtests/*.webp

# Large source-data archives (rebuild/redownload; like spy.zip)
/data/
*.zip
SPY_1hour_*.csv

# Service & staging dirs (run separately / not part of this repo)
incoming/
upload_service/
protected_downloads/

# Generated / scratch / scratch dirs
audit-reruns/
agent_reports/
archive/
massive_pipeline/
outputs/
.hermes/
*.log
*.pre-sanitize

# Heavy per-event dumps (regenerable from backtests; too big for git)
/tenam_traffic_events.json
analyst/bilbo_box_events.csv
analyst/es_po_comp_drift_events.csv

# ThetaData terminal (42MB jar + runtime; auth happens outside the repo)
/theta/
analyst/po_comp_options/theta/

# Options pipeline raw data & intermediates (19GB+; regenerable via
# fetch_po_comp_options.py / scratch_run_*.sh pipelines)
analyst/po_comp_options/option_bars.sqlite
analyst/po_comp_options/chains/
analyst/po_comp_options/*.parquet
analyst/po_comp_options/contracts_todo*.json

# Raw fetched market data (regenerable via fetch scripts)
analyst/forward_2026/
analyst/ipo_study/bars/
analyst/ipo_study/bars_yahoo/
analyst/ipo_study/curve_bars_cache.pkl
analyst/*_1m_massive_fwd.csv

# Monte-carlo path dumps and tuning grids (intraday momentum study)
analyst/intraday_momentum/*.npz
analyst/intraday_momentum/*.parquet

# Bilbo scanner runtime state (mutated by cron every 2 minutes)
analyst/bilbo_scanner/state.json

# OS
.DS_Store
Thumbs.db
.codex

# Site scratch (FAL/Kling video experiments — not part of the published site)
site/run_part*.py
site/part*.log
site/concat.txt

# Site large binaries — kept on disk for nginx, regenerable, not for git
site/data/*.csv.gz
site/data/*.csv.gz.part_*
site/*.mp4
site/*.gif
site/data/scripts.zip

# Analyst service — never commit (lives separately, runs on PM2)
analyst/server.py
analyst/extension/
site/analyst.html

# Weekly plan charts (keep)
!weekly-plans/*.jpg
!weekly-plans/*.png

# nohup / stray process output
*.out
nohup.out
