## Cooperation contract — GG bug fix + bilbo demotion (2026-04-26)

Two agents working in parallel on related tasks. Read this whole file before starting your task. Update your STATUS section every time you complete a milestone. **Do not modify the other agent's section.**

---

### Background

Both Codex 5.5 and Opus 4.7 confirmed two findings:

1. `backtest_gg_with_po.py` has a look-ahead bug. `merge_asof(direction="backward")` against left-labeled 1h bars from `aggregate.py:57-66` lets a 10m trigger at 09:40 see the not-yet-closed 09:00–09:59 hour. Up to 60 min of forward leakage. Documented in `audit-reruns/codex_gg_bug_review_2026-04-26.md` and `audit-reruns/opus_gg_bug_review_2026-04-26.md`.
2. The published Subway 80% is inflated. The honest "open inside, cross intraday" cohort is ~52% bull / ~56% bear (Codex reproduced 1224/2355 = 52.0% bull, 1325/2339 = 56.6% bear).

Codex's quantified impact when joins are made point-in-time safe:
- Bear PO Low+Falling: **89.6% → 85.1%**
- Bull PO High+Rising: 75.9% → 71.0%
- Bull PO Mid+Falling: 50.9% → 63.0% (worst bucket partly inverts)
- Bear PO Mid+Rising: 54.5% → 69.6%

**bilbo-golden-gate is being demoted from KEEPER → DRAFT.** All published PO-conditioned numbers are biased upward and the flagship study can't be tradable in its current form.

---

### Coordination rules

- Site root is `/root/spy/site/` (served live at https://milkmantrades.com via nginx — no build step, edits go live immediately).
- Repo root is `/root/spy/` (git remote `origin` = `prsdro/spy` on `main`).
- Don't `git push` — that's the orchestrator's job. Just commit locally if you want, or leave staged. The orchestrator will batch the final push.
- Don't touch each other's files (see "scope" in each section).
- If you discover the other agent's scope needs a change, write a NOTE under your STATUS section. Don't edit their files.
- America/Chicago timezone for any dates.

---

## Agent A — Codex 5.5: Fix the bug + rerun stats

### Scope (FILES YOU MAY EDIT)
- `/root/spy/backtest_gg_with_po.py` — fix the join
- `/root/spy/aggregate.py` — only if needed for bar-end labels (preferred: fix at the consumer side, not the producer)
- `/root/spy/audit-reruns/gg_bug_fix_results_2026-04-26.md` — your results doc (CREATE)
- `/root/spy/audit-reruns/postfix-stats_2026-04-26/` — any output JSONs/CSVs from your rerun (CREATE dir)

### DO NOT TOUCH
- `/root/spy/site/` (Opus is updating the website)
- `/root/spy/audit-reruns/milkman_editorial_audit_2026-04-26.md` (Opus may update)
- Any other backtest scripts unless they share the same join bug — if so, list them in your NOTES, don't fix yet.

### Task
1. Implement the point-in-time-safe fix in `backtest_gg_with_po.py`. Reference pattern: `backtest_bilbo_box_htf_po_exits.py:126-145` already does it correctly (shift HTF timestamps forward by one full bar duration before `merge_asof`).
2. Re-run the full GG-with-PO study on the published 2000-2025 window.
3. Produce honest residual stats for both legs:
   - 23.6 → 38.2 (Trigger leg) with Cat A / B / residual breakdown
   - 38.2 → 61.8 (GG completion leg) with Cat A / B / residual breakdown
4. Re-run with PO buckets (zone × slope × state) under the fixed join. Compare side-by-side with the published numbers.
5. Save outputs (JSON + CSV) under `/root/spy/audit-reruns/postfix-stats_2026-04-26/`.
6. Write `gg_bug_fix_results_2026-04-26.md` with:
   - Diff of the fix (before/after code)
   - Side-by-side stat tables (published vs corrected)
   - Verification that the fix doesn't break other studies that already do it right
   - List of OTHER backtests that may share the same bug (don't fix — flag in a "follow-up" section)

### STATUS (update as you go)
- [x] Bug confirmed + fix designed
- [x] Fix applied to backtest_gg_with_po.py
- [x] Trigger-leg (23.6→38.2) rerun complete
- [x] GG-completion-leg (38.2→61.8) rerun complete
- [x] PO bucket rerun complete
- [x] Results doc written
- [x] Other affected scripts cataloged

### NOTES (free-form)
- Saved rerun artifacts under `/root/spy/audit-reruns/postfix-stats_2026-04-26/`.
- Used the published `2000-2025` window explicitly as `timestamp < 2026-01-01 00:00:00` because `spy.db` now extends into 2026.
- Follow-up candidates cataloged in `gg_bug_fix_results_2026-04-26.md`: exact bug in `server.py` and `export_study_dates.py`; same start-labeled 1h merge pattern in `backtest_gg_entries.py` and `backtest_gg_invalidation.py`.

---

## Agent B — Opus 4.7: Demote bilbo-gg + update site

### Scope (FILES YOU MAY EDIT)
- `/root/spy/site/index.html` — remove bilbo-golden-gate from Day Trading core, renumber 02→07
- `/root/spy/site/drafts.html` — add bilbo-golden-gate as 9th draft entry with bug + Subway-inflation reasoning
- `/root/spy/site/bilbo-golden-gate.html` — add a draft banner at top of page (style consistent with site)
- `/root/spy/site/golden-gate.html` — add an honest "intraday-cross residual" callout (52% bull / 56% bear) to the Subway level-to-level section, OR demote it too if it's too contaminated
- `/root/spy/site/cheatsheet-bilbo.html` — add draft banner OR archive (your call after reviewing it)
- `/root/spy/site/cheatsheet.html` — update master cheat sheet links if anything moved
- `/root/spy/audit-reruns/milkman_editorial_audit_2026-04-26.md` — append an addendum noting the demotion + reasoning
- `/root/spy/audit-reruns/cheatsheet_audit_2026-04-26.md` — append addendum if cheat sheets changed

### DO NOT TOUCH
- `/root/spy/backtest_gg_with_po.py`, `/root/spy/aggregate.py` (Codex is fixing)
- `/root/spy/audit-reruns/codex_gg_bug_review_2026-04-26.md` or `opus_gg_bug_review_2026-04-26.md` (read-only, those are the source-of-truth review docs)
- `/root/spy/audit-reruns/gg_bug_fix_results_2026-04-26.md` (Codex writes)
- Any other study .html pages (only the ones listed above)

### Task
1. **Homepage demotion.** In `site/index.html`:
   - Remove `bilbo-golden-gate.html` from the Core Playbook card list (currently #02)
   - Renumber the remaining keepers (current 03→07 become 02→06)
   - Day Trading section drops from 10 cards to 9
   - Update the section-intro text to reflect "top FIVE" or rephrase appropriately

2. **Drafts page.** In `site/drafts.html`:
   - Bump count from "8 studies" to "9 studies"
   - Add a new study-card for `bilbo-golden-gate` near the top of the drafts list (it's the most consequential demotion). Reasoning text should be approximately: *"Look-ahead bias confirmed. The 1h Phase Oscillator is sampled from the not-yet-closed current hour bar — up to 60 min of forward leakage. Point-in-time-safe rerun shows the flagship 90.3% bear cohort is closer to ~85%, and the bull/mid buckets partly invert. Needs full rebuild with bar-end PO joins before it goes back in the playbook."*

3. **Study page banner.** Add a draft banner to `site/bilbo-golden-gate.html` (right under the page title): an amber-tinted notice saying the study is being rebuilt due to a confirmed look-ahead bug, with a link to `/drafts.html#bilbo-golden-gate` for full reasoning. Use the existing site CSS palette (--accent #c9f76f, amber #fbbf24, dark theme). Keep it tasteful, one short paragraph.

4. **Subway page (`golden-gate.html`).** This page publishes the level-to-level table. Read it. The "Trigger → 38.2 = 80%" headline conflates Cat A + Cat B + residual. Add a clearly-labeled "**Honest residual (open inside, crosses intraday)**" callout: ~52% bull / ~56% bear. Keep the original headline number but contextualize it. Don't pull the whole page — Codex's review notes the 80% Subway figure is a different leg than the 38.2→61.8 numbers, so the page itself is salvageable with proper framing.

5. **Cheat sheet.** Read `cheatsheet-bilbo.html` first. If it's primarily about bilbo-golden-gate setups, archive it (rename to `_archive_cheatsheet-bilbo.html`) and remove the link from `cheatsheet.html`. If it covers other bilbo content too, add a draft banner instead.

6. **Audit doc addenda.** Append a clearly-dated section to `milkman_editorial_audit_2026-04-26.md` titled "Addendum 2026-04-26 evening — bilbo-golden-gate demoted to DRAFT" with a 5-10 line summary of why (cite the codex/opus review docs).

7. **Verify.** After all changes, `curl -sI` every URL on the homepage and drafts page. Anything not 200 = fix it.

### STATUS (update as you go)
- [x] Homepage updated + renumbered
- [x] Drafts page updated (9 entries)
- [x] bilbo-golden-gate.html banner added
- [x] golden-gate.html honest residual callout added
- [x] cheatsheet-bilbo.html handled
- [x] cheatsheet.html links updated (if needed)
- [x] Editorial audit addendum appended
- [x] All URLs verified 200

### NOTES (free-form)
- Archived `cheatsheet-bilbo.html` → `_archive_cheatsheet-bilbo.html` (entire sheet was bilbo-GG content; not salvageable without rerun). Link removed from master cheat sheet, positions renumbered 02→05.
- Did not touch any backtest scripts. Codex's domain.
