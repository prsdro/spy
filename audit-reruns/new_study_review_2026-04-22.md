- [High] Quarterly regime split is not a clean Saty `3M` prior-quarter context. `build_quarterly_context()` resamples Sunday-stamped weekly bars into calendar quarters, so quarter boundaries are wrong: e.g. Q1 2026 close becomes the 2026-03-27 weekly close `634.08`, not the 2026-03-31 daily close `650.235`. The Friday-based lookup also assigns new-quarter context to taps before quarter start in straddling weeks. A daily/tap-date reconstruction changed 182/654 valid tap q-bands (27.8%). Fix: build quarters from `candles_1d` calendar quarters and select the last completed quarter as of `tap_time` or explicit week start. Ref: `backtest_multiday_put_trigger_reversion.py:164`, `:654`.
- [High] `intraweek_cross` includes non-tradable gap-through minutes. The tap condition only requires `low <= put_trigger`; 62/760 tap bars have `high < put_trigger`, so the recorded trigger entry never printed in that 1m range. These gap-below cases are much worse (16.1% pivot, 83.9% breakdown) and pull the headline from 38.0% to 36.2% pivot. Fix: require the tap bar range to contain the trigger for trigger-entry trades; put later-session gaps below trigger into a separate cohort or enter at first attainable price. Ref: `backtest_multiday_put_trigger_reversion.py:266`, `:273`.
- [Medium] Raw 1m bad wicks are used, bypassing the repo RTH clipping convention. The known 2025-07-03 phantom low (`581.8076`) is counted as an intraweek tap/breakdown; 2009-06-02 has the same issue. Applying the repo's 2% body wick clip changed 3 weeks and removed 2 false tap events (`760 -> 758`). Fix: clip `candles_1m` RTH bars the same way as `aggregate.py`, or consume a sanitized table. Ref: `backtest_multiday_put_trigger_reversion.py:213`.
- [Medium] Call/UGG continuation and trade stats ignore stops after pivot. Classification checks whether `dgg_time` beats `pivot_time`, then ignores `dgg_time` for `call_time`/`ugg_time`; 18 call wins and 9 full rotations have `dgg_time` before the later upside target. Fix target-specific races as `target_time < dgg_time` or no `dgg_time`. Ref: `backtest_multiday_put_trigger_reversion.py:311`.
- [Medium] Trade expectancy treats every `no_resolution` as `-1R`. There are 26 intraweek-cross no-resolution events with neither pivot nor dgg hit; counting them as full stop-outs is conservative but not an actual trade simulation. Fix: add a Friday close/time-stop R result, or report both closed-target expectancy and unresolved-excluded expectancy. The R ratios themselves are correct for trigger entry to dgg stop. Ref: `backtest_multiday_put_trigger_reversion.py:591`.
- [Medium] `wilder_atr()` is not exact SMA-seeded Wilder RMA despite the comment. `ewm(..., adjust=False, min_periods=14)` hides early values but does not seed the first ATR with an SMA as TradingView `ta.rma/ta.atr` does. Weekly max early drift was 1.24 ATR points (15.4%) and a true SMA-seeded rerun changed 3 early outcomes. Fix: explicitly seed with the first 14 TR SMA if "match TradingView verbatim" is required. Ref: `backtest_multiday_put_trigger_reversion.py:78`.
- [Low] Weekly Friday-to-Sunday lookup is correct, but the output date is mislabeled. The code maps trading Friday `F` to the Sunday row `F+2`, then uses shifted prior-week anchors; no one-off shift found. But CSV `week_end` stores the Sunday weekly-bar timestamp, not the actual Friday trading-week end, which can shift year/decade grouping. Fix: output both `trading_week_end` and `weekly_bar_ts`. Ref: `backtest_multiday_put_trigger_reversion.py:641`, `:344`.
- [Low] The latest output includes a partial final week. `candles_1m` ends at `2026-04-09 19:59:00`, but the CSV includes weekly row `2026-04-12`; that week has no Apr 10 RTH data. The current event had already full-rotated, so the headline likely survives, but the runner should drop incomplete final weeks or enforce expected sessions.
- [Low] `results.json` leaves `decade_splits` and `trade_simulation` empty even though the CLI prints them and the headline cites trade expectancy. Fix: populate those JSON sections or remove the empty keys before publication. Ref: `backtest_multiday_put_trigger_reversion.py:700`.
- [Info] No issue found on `dgg_time is None and pivot_time is None`: `dgg_time` uses `low <= dgg_open`, so an exact dgg touch is captured as breakdown under the loaded 1m data. Ref: `backtest_multiday_put_trigger_reversion.py:292`.
- [Info] Exact cohort boundary behavior is deterministic: `open == dgg` -> `opened_below_dgg`, `open == put_trigger` -> `opened_below_trigger`, and `week_low == put_trigger` -> `intraweek_cross`. No bug unless the intended semantics require strict crosses instead of touches. Ref: `backtest_multiday_put_trigger_reversion.py:261`.
